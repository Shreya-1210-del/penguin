# Penguin Intelligence Backend

FastAPI service for resource-demand ML forecasting and the grounded Penguin AI assistant.

Run from this directory:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
